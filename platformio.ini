; Entorno de compilacion de prueba para M5PaperSeedWorkstation.
; El build real se hace con Arduino IDE; esto solo sirve para validar que compila.
; Uso: ~/.platformio/penv/bin/pio run -e native
; La libreria M5EPD esta en ~/Documents/Arduino/libraries.

[platformio]
src_dir = .
default_envs = native

[env:native]
platform = espressif32@5.0.0
board = m5stack-fire
framework = arduino
lib_extra_dirs = ~/Documents/Arduino/libraries/M5EPD
lib_deps = h2zero/NimBLE-Arduino@^1.4.0
monitor_speed = 115200
upload_speed = 115200
upload_flags = --no-stub
build_flags =
    -DARDUINO_M5STACK_PAPER
